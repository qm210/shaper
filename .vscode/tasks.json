{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build ${cmake.buildDirectory} --config Debug --target TARGET_TO_BUILD -- -j 4",
            "options": {
                "cwd": "${cmake.buildDirectory}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}